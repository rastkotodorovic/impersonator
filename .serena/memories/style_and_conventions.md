Project follows Laravel conventions and PSR-12. Use 4-space indentation, one class per file, PascalCase classes, camelCase methods/variables, and snake_case database columns/migration names. Keep controllers thin and place integration logic in app/Services. Format PHP with Pint. Tests are split between tests/Feature for request/route behavior and tests/Unit for isolated service logic. Avoid committing secrets from .env.